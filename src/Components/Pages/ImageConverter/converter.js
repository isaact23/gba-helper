
/**
 * Given an array of the RGB values of an image and a conversion mode,
 * generate a header file formatted for GBA.
 */
export function convert(pixels, mode) {
    if (mode === "bitmapRaw") {
        console.log("Now converting " + pixels.shape.slice() + " with mode " + mode);
    }
    return null;
}
